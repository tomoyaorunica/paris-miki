
import _ from '@/js/utils/Util'
import Event from '@/js/events/Event'
import EventEmitter from '@/js/events/EventEmitter'

export default class HeaderView
{
  constructor()
  {
    return

    // this.header = _.selector(".p-header")
    // if(!this.header) return

    // this.toggle = _.selector(".p-header-toggle")

    // this.li = _.selectorAll(".p-header nav ul li")
    // this.li.forEach( target =>
    // {
    //   new LightningBtn(target, "header")
    // })
    
    // EventEmitter.ee.on("show-header-without-loading", () =>
    // {
    //   _.addClass(this.header, "-show-static")
    //   _.addClass(this.toggle, "-show-static")
    // })
    
    // EventEmitter.ee.on("loading-end", () =>
    // {
    //   _.addClass(this.header, "-show-slide")
    //   _.addClass(this.toggle, "-show-slide")
    // })

    // Event.add(this.listen.bind(this))
  }

	// listen(param)
	// {
	// 	switch (param)
	// 	{
	// 		case Event.SCROLL :
	// 			this.scroll()
	// 			break;
	// 	}
	// }

  // scroll()
  // {
  //   if(Event.size.sy > 60)
  //   {
  //     _.addClass(this.header, "-small")
  //   }
  //   else
  //   {
  //     _.removeClass(this.header, "-small")
  //   }
  // }
}